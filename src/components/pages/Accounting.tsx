import React from "react";

interface IAccountingProps { }

export const Accounting: React.FC<IAccountingProps> = (): JSX.Element => {
	return (
		<div className="accounting">
			Accounting
		</div>
	)
}